# VERSION=0.1.0

# common fields
data_name: "DeepGlint_asian"
data_dir: "/datasets/face/DeepGlint_asian_mask_insert"
cache_dir: "/datasets/eval_cache/data"
prefix: "data:mask_insert"

# cache
create_cache: False
min_imgs: 10  # số lượng tối thiểu của ảnh trong cls

# pairs
create_pairs: False
total: 1.0e+8
positive_ratio: 0.1
buffer_size: 1.0e+7

# feature_exaction
extract_feature: True
data_type: "face_folder"
onnx_path: "/research/classification/FaceX-Zoo/weights/Epoch_17.onnx"
emb_dim: 512
model_name: "facexzoo_mbfn"